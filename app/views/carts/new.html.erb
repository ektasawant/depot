<html>
<head>
	<title>Carts</title>
</head>
<h1>New cart </h1>
<%= form_for(@cart,:url=> { :action=> "create" }) do |f| %>


  <% if @cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
      <% @cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  #<% end %>

<p><%= f.submit "Create" %></p>

<% end %>
</html>