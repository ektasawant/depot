$('#notice').hide();
if ($('#cart tr').length == 1) { $('#cart').show('blind', 1000); }
$('#cart').html("<%= escape_javascript render(@cart) %>");
//(selector).html("format the element and return new elenent")
$('#current_item').css({'background-color':'#88ff88'}).animate({'background-color':'#114411'}, 1000);


  // call current_item selector with css  for changing background-color 
  //and animate that background-color for 1000 millisecond i.e 1 second